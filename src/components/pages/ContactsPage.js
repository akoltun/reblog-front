import React from 'react';

import { Segment } from 'semantic-ui-react';
import Helmet from 'react-helmet';

const ContactsPage = () => (
  <div>
    <Helmet
      title="Обратная связь"
    />
    <Segment>This is contacts page</Segment>
  </div>
);

export default ContactsPage;
